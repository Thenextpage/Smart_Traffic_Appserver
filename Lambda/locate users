# Using this code the traffic light will be able to locate specific users that are inside a specific polygon, which will be described as a set of coordinates.

import matplotlib.path as mplPath
import numpy as np

bbPath = mplPath.Path(np.array([[1,2],[5,1],[9,4],[4,6]]))

print( bbPath.contains_point((4,3)))
